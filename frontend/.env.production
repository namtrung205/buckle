# This value is injected at Docker build time via ARG VITE_BACKEND_SERVER in the Dockerfile.
# Leave this file empty or set it for local production builds only.
# VITE_BACKEND_SERVER=https://your-domain.com/api
VITE_BACKEND_SERVER=