[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]

# Rebuild std for WASM to enable JS-backed Instant::now() (fixes time panic)
[unstable]
build-std = ["std", "panic_abort"]
